<?xml version="1.0" encoding="UTF-8" standalone="no" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment> MemberDAO SQL Query </comment>

    <!-- 아이디 중복 확인 -->
    <entry key="checkDuplicateId">
        SELECT count(*) AS member_count
        FROM tbl_member
        WHERE login_id = ?
    </entry>

    <!-- 회원 가입 -->
    <entry key="insertMemberJoin">
        INSERT INTO tbl_member (grade_code, login_id, password, nickname, phone)
        VALUES(1, ?, ?, ?, ?)
    </entry>

    <!-- 로그인 -->
    <entry key="checklogin">
        SELECT *
        FROM tbl_member
        WHERE login_id = ?
        AND password = ?
    </entry>

    <!-- 내정보 조회 -->
    <entry key="selectMemberInfo">
        SELECT login_id, password, nickname,
        phone, point_balance, total_amount
        FROM tbl_member
        WHERE member_code = ?
    </entry>

    <!-- 내정보 수정-->
    <entry key="modifyMemberInfo">
        UPDATE tbl_member
        SET password = ?,
        nickname = ?,
        phone = ?
        WHERE member_code = ?
    </entry>

    <!-- 내 계정 탈퇴(보류)-->
    <entry key="deleteMember">
        DELETE
        FROM tbl_member
        WHERE member_code = ?
    </entry>

    <entry key="selectGradeName">
        SELECT grade_name
        FROM tbl_membership_grade
        WHERE grade_code = ?
    </entry>

</properties>