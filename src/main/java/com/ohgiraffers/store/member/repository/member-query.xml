<?xml version="1.0" encoding="UTF-8" standalone="no" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment> MemberDAO SQL Query </comment>

    <!-- 아이디 중복 확인 -->
    <entry key="selectMemberByLoginId">
        SELECT count(*) AS 'ID 중복 확인'
        FROM tbl_member
        WHERE login_id = ? ;
    </entry>

    <!-- 회원 가입 -->
    <entry key="insertMemberJoin">
        INSERT INTO tbl_member (grade_code, login_id, password, nickname, phone)
        VALUES(1, ?, ?, ?, ?);
    </entry>

    <entry key="">
        SELECT password
        FROM tbl_member
        WHERE login_id = ?;
    </entry>

    <entry key="">
        SELECT nickname, login_id, password,
        phone, point_balance, total_amount
        FROM tbl_member
        WHERE member_code = ?;
    </entry>

    <!-- 포인트 적립  -->
    <entry key="">
        UPDATE tbl_member AS a
        JOIN tbl_membership_grade AS b
        ON a.grade_code = b.grade_code
        JOIN tbl_payment AS c
        ON a.member_code = c.member_code
        SET point_balance = reward_rate * final_amount
        WHERE payment_status = 'COMPLETED';
    </entry>

</properties>